{
  "name": "bigbrain-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Big Brain — drop-anything reference inbox (Cloudflare Worker).",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "FAIL=; for f in test/*.test.mjs ../local/test.mjs; do echo \"--- $f\"; node \"$f\" || FAIL=1; done; [ -z \"$FAIL\" ]",
    "doctor": "node scripts/doctor.mjs",
    "setup-kv": "node scripts/setup-kv.mjs",
    "notion-import": "node scripts/notion-import.mjs",
    "ig-join": "node scripts/ig-join.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.115.0"
  }
}
